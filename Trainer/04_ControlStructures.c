// #include <stdio.h>

// int main()
// {
//     int a = 10, b = 50;

//     int guess = 35;

//     // Accept a number from the User


//     if (a < 5)
//     {
//         // operations
//         // interaction
//         // Save data
//     }
//     else
//         // single statement

//     // DE-recommended style -> if (cond) {stmt1; stmt2; stmt3;} else stmt4;

//     printf("Value is %s than 5", (a < 5 ? "less" : "not less"));

//     int ret = func();
//     if (guess < a)
//         printf("guess is less than a");
//     else if(guess > b)
//         printf("guess is greater than b");
//     else if ((guess != a) && (guess != b))
//     else if (TRUE || (guess != ret))
//         prinf(" guess lies between a and b, exclusive range");
    
// //Short circuit evalution

//     return 0;
// }



int main()
{
    // Accept the month from user asn store as an integer
    int month = 4;

    switch(month)
    {
        case 1:
            //operations
            break;
        case 2:
            //operations
            break;
        case 3:
            //operations
            break;
        case 4:
            //operations
            break;
        case 5:
            //operations
            break;
        default:
            // Operations for case if month did not match any of the given choices
    }


    int x, y;
    for ( x = 0, y = 100  ;  x>y; x += 1, y -= 1 );

    // operations
    // iterations with a counter

    while(y > 0) // Till condition is breached
    {
        //operations
        y -= 1;
    }

    do
    {
        //Execute at least once
    } while (/* condition */);
    

}